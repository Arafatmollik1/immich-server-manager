{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "dialog-permissions",
  "description": "Allow the app to open native folder pickers",
  "windows": ["main"],
  "permissions": ["dialog:default", "dialog:allow-open"]
}
